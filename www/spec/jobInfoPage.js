/*
 * JS for jobInfoPage generated by Appery.io
 *
 */

Appery.getProjectGUID = function() {
    return 'f3315dc4-25fd-4d54-9cf7-664bdfeb1353';
}

function navigateTo(outcome, useAjax) {
    Appery.navigateTo(outcome, useAjax);
}

function adjustContentHeight() {
    Appery.adjustContentHeight();
}

function adjustContentHeightWithPadding() {
    Appery.adjustContentHeightWithPadding();
}

function setDetailContent(pageUrl) {
    Appery.setDetailContent(pageUrl);
}

Appery.AppPages = [{
    "name": "jobInfoPage",
    "location": "jobInfoPage.html"
}, {
    "name": "searchResultPage",
    "location": "searchResultPage.html"
}, {
    "name": "searchJobPage",
    "location": "searchJobPage.html"
}];

jobInfoPage_js = function(runBeforeShow) { /* Object & array with components "name-to-id" mapping */
    var n2id_buf = {
        'mobilelabel_10': 'jobInfoPage_mobilelabel_10',
        'mobilelabel_3': 'jobInfoPage_mobilelabel_3',
        'html_8': 'jobInfoPage_html_8',
        'mobilelabel_9': 'jobInfoPage_mobilelabel_9',
        'mobilelabel_11': 'jobInfoPage_mobilelabel_11'
    };

    if ("n2id" in window && window.n2id !== undefined) {
        $.extend(n2id, n2id_buf);
    } else {
        window.n2id = n2id_buf;
    }

    if (navigator.userAgent.indexOf("IEMobile") != -1) {
        //Fixing issue https://github.com/jquery/jquery-mobile/issues/5424 on Windows Phone
        $("div[data-role=footer]").css("bottom", "-36px");
    }

    Appery.CurrentScreen = 'jobInfoPage';

    /*
     * Nonvisual components
     */
    var datasources = [];

    /*
     * Events and handlers
     */

    // Before Show
    jobInfoPage_beforeshow = function() {
        Appery.CurrentScreen = "jobInfoPage";
        for (var idx = 0; idx < datasources.length; idx++) {
            datasources[idx].__setupDisplay();
        }
    }

    // On Load
    screen_04D3_onLoad = jobInfoPage_onLoad = function() {
        screen_04D3_elementsExtraJS();

        // TODO fire device events only if necessary (with JS logic)
        jobInfoPage_deviceEvents();
        jobInfoPage_windowEvents();
        screen_04D3_elementsEvents();
    }

    // screen window events
    screen_04D3_windowEvents = jobInfoPage_windowEvents = function() {
        $('#jobInfoPage').bind('pageshow orientationchange', function() {
            adjustContentHeightWithPadding();
        });

    }

    // device events
    jobInfoPage_deviceEvents = function() {

        document.addEventListener("deviceready", function() {

        });
    }

    // screen elements extra js
    screen_04D3_elementsExtraJS = jobInfoPage_elementsExtraJS = function() {
        // screen (jobInfoPage) extra code

    }

    // screen elements handler
    screen_04D3_elementsEvents = jobInfoPage_elementsEvents = function() {

        $("a :input,a a,a fieldset label").live({
            click: function(event) {
                event.stopPropagation();
            }
        });

    }

    $("#jobInfoPage").die("pagebeforeshow").live("pagebeforeshow", function(event, ui) {
        jobInfoPage_beforeshow();
    });

    if (runBeforeShow) {
        jobInfoPage_beforeshow();
    } else {
        jobInfoPage_onLoad();
    }

}

$("#jobInfoPage").die("pageinit").live("pageinit", function(event, ui) {
    Appery.processSelectMenu($(this));
    jobInfoPage_js();
});