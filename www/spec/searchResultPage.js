/*
 * JS for searchResultPage generated by Appery.io
 *
 */

Appery.getProjectGUID = function() {
    return 'f3315dc4-25fd-4d54-9cf7-664bdfeb1353';
}

function navigateTo(outcome, useAjax) {
    Appery.navigateTo(outcome, useAjax);
}

function adjustContentHeight() {
    Appery.adjustContentHeight();
}

function adjustContentHeightWithPadding() {
    Appery.adjustContentHeightWithPadding();
}

function setDetailContent(pageUrl) {
    Appery.setDetailContent(pageUrl);
}

Appery.AppPages = [{
    "name": "jobInfoPage",
    "location": "jobInfoPage.html"
}, {
    "name": "searchResultPage",
    "location": "searchResultPage.html"
}, {
    "name": "searchJobPage",
    "location": "searchJobPage.html"
}];

searchResultPage_js = function(runBeforeShow) { /* Object & array with components "name-to-id" mapping */
    var n2id_buf = {
        'mobilesearchbar_2': 'searchResultPage_mobilesearchbar_2',
        'mobilelist_3': 'searchResultPage_mobilelist_3',
        'mobilelistitem_4': 'searchResultPage_mobilelistitem_4',
        'mobilelistitembutton_5': 'searchResultPage_mobilelistitembutton_5',
        'mobilelistitem_6': 'searchResultPage_mobilelistitem_6',
        'mobilelistitembutton_7': 'searchResultPage_mobilelistitembutton_7',
        'mobilelistitem_8': 'searchResultPage_mobilelistitem_8',
        'mobilelistitembutton_9': 'searchResultPage_mobilelistitembutton_9'
    };

    if ("n2id" in window && window.n2id !== undefined) {
        $.extend(n2id, n2id_buf);
    } else {
        window.n2id = n2id_buf;
    }

    if (navigator.userAgent.indexOf("IEMobile") != -1) {
        //Fixing issue https://github.com/jquery/jquery-mobile/issues/5424 on Windows Phone
        $("div[data-role=footer]").css("bottom", "-36px");
    }

    Appery.CurrentScreen = 'searchResultPage';

    /*
     * Nonvisual components
     */
    var datasources = [];

    /*
     * Events and handlers
     */

    // Before Show
    searchResultPage_beforeshow = function() {
        Appery.CurrentScreen = "searchResultPage";
        for (var idx = 0; idx < datasources.length; idx++) {
            datasources[idx].__setupDisplay();
        }
    }

    // On Load
    screen_8216_onLoad = searchResultPage_onLoad = function() {
        screen_8216_elementsExtraJS();

        // TODO fire device events only if necessary (with JS logic)
        searchResultPage_deviceEvents();
        searchResultPage_windowEvents();
        screen_8216_elementsEvents();
    }

    // screen window events
    screen_8216_windowEvents = searchResultPage_windowEvents = function() {
        $('#searchResultPage').bind('pageshow orientationchange', function() {
            adjustContentHeightWithPadding();
        });

    }

    // device events
    searchResultPage_deviceEvents = function() {

        document.addEventListener("deviceready", function() {

        });
    }

    // screen elements extra js
    screen_8216_elementsExtraJS = searchResultPage_elementsExtraJS = function() {
        // screen (searchResultPage) extra code

        /* mobilelist_3 */

        listView = $("#searchResultPage_mobilelist_3");
        theme = listView.attr("data-theme");
        if (typeof theme !== 'undefined') {
            var themeClass = "ui-btn-up-" + theme;
            listItem = $("#searchResultPage_mobilelist_3 .ui-li-static");
            $.each(listItem, function(index, value) {
                $(this).addClass(themeClass);
            });
        }

        /* mobilelistitem_4 */

        /* mobilelistitem_6 */

        /* mobilelistitem_8 */

    }

    // screen elements handler
    screen_8216_elementsEvents = searchResultPage_elementsEvents = function() {

        $("a :input,a a,a fieldset label").live({
            click: function(event) {
                event.stopPropagation();
            }
        });

    }

    $("#searchResultPage").die("pagebeforeshow").live("pagebeforeshow", function(event, ui) {
        searchResultPage_beforeshow();
    });

    if (runBeforeShow) {
        searchResultPage_beforeshow();
    } else {
        searchResultPage_onLoad();
    }

}

$("#searchResultPage").die("pageinit").live("pageinit", function(event, ui) {
    Appery.processSelectMenu($(this));
    searchResultPage_js();
});