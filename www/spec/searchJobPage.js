/*
 * JS for searchJobPage generated by Appery.io
 *
 */

Appery.getProjectGUID = function() {
    return 'f3315dc4-25fd-4d54-9cf7-664bdfeb1353';
}

function navigateTo(outcome, useAjax) {
    Appery.navigateTo(outcome, useAjax);
}

function adjustContentHeight() {
    Appery.adjustContentHeight();
}

function adjustContentHeightWithPadding() {
    Appery.adjustContentHeightWithPadding();
}

function setDetailContent(pageUrl) {
    Appery.setDetailContent(pageUrl);
}

Appery.AppPages = [{
    "name": "jobInfoPage",
    "location": "jobInfoPage.html"
}, {
    "name": "searchResultPage",
    "location": "searchResultPage.html"
}, {
    "name": "searchJobPage",
    "location": "searchJobPage.html"
},	{
    "name": "startScreen",
    "location": "startScreen.html"
}];

searchJobPage_js = function(runBeforeShow) { /* Object & array with components "name-to-id" mapping */
    var n2id_buf = {
        'mobilesearchbar_2': 'searchJobPage_mobilesearchbar_2'
    };

    if ("n2id" in window && window.n2id !== undefined) {
        $.extend(n2id, n2id_buf);
    } else {
        window.n2id = n2id_buf;
    }

    if (navigator.userAgent.indexOf("IEMobile") != -1) {
        //Fixing issue https://github.com/jquery/jquery-mobile/issues/5424 on Windows Phone
        $("div[data-role=footer]").css("bottom", "-36px");
    }

    Appery.CurrentScreen = 'searchJobPage';

    /*
     * Nonvisual components
     */
    var datasources = [];

    /*
     * Events and handlers
     */

    // Before Show
    searchJobPage_beforeshow = function() {
        Appery.CurrentScreen = "searchJobPage";
        for (var idx = 0; idx < datasources.length; idx++) {
            datasources[idx].__setupDisplay();
        }
    }

    // On Load
    screen_3671_onLoad = searchJobPage_onLoad = function() {
        screen_3671_elementsExtraJS();

        // TODO fire device events only if necessary (with JS logic)
        searchJobPage_deviceEvents();
        searchJobPage_windowEvents();
        screen_3671_elementsEvents();
    }

    // screen window events
    screen_3671_windowEvents = searchJobPage_windowEvents = function() {
        $('#searchJobPage').bind('pageshow orientationchange', function() {
            adjustContentHeightWithPadding();
        });

    }

    // device events
    searchJobPage_deviceEvents = function() {

        document.addEventListener("deviceready", function() {

        });
    }

    // screen elements extra js
    screen_3671_elementsExtraJS = searchJobPage_elementsExtraJS = function() {
        // screen (searchJobPage) extra code

    }

    // screen elements handler
    screen_3671_elementsEvents = searchJobPage_elementsEvents = function() {

        $("a :input,a a,a fieldset label").live({
            click: function(event) {
                event.stopPropagation();
            }
        });

    }

    $("#searchJobPage").die("pagebeforeshow").live("pagebeforeshow", function(event, ui) {
        searchJobPage_beforeshow();
    });

    if (runBeforeShow) {
        searchJobPage_beforeshow();
    } else {
        searchJobPage_onLoad();
    }

}

$("#searchJobPage").die("pageinit").live("pageinit", function(event, ui) {
    Appery.processSelectMenu($(this));
    searchJobPage_js();
});